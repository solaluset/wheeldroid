diff --git a/setup.py b/setup.py
index ea87da7..b747e3b 100644
--- a/setup.py
+++ b/setup.py
@@ -136,6 +136,7 @@ class build_clib(_build_clib):
             "--disable-debug",
             "--disable-dependency-tracking",
             "--with-pic",
+            "--host=aarch64-linux-android24",
         ]
         if platform.system() == "SunOS":
             # On Solaris, libssp doesn't link statically and causes linker
@@ -154,11 +155,6 @@ class build_clib(_build_clib):
         # Build the library
         subprocess.check_call([make_command] + make_args, cwd=build_temp)
 
-        # Check the build library
-        subprocess.check_call(
-            [make_command, "check"] + make_args, cwd=build_temp
-        )
-
         # Install the built library
         subprocess.check_call(
             [make_command, "install"] + make_args, cwd=build_temp
